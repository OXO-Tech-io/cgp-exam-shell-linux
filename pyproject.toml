[project]
name = "cgp-exam-shell-linux"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "evdev>=1.9.3",
    "pynput>=1.8.2",
    "pyside6>=6.11.1",
    "pyside6-addons>=6.11.1",
    "python-xlib>=0.33",
]

[dependency-groups]
# Build-time only -- freezes the app into installer/.build so the .deb ships a
# self-contained binary. Never installed on a candidate's machine.
dev = ["pyinstaller>=6.10"]
